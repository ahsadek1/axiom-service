## 🚨 SELF-HEALTH MANDATE — RUNS BEFORE ANYTHING ELSE

Before reading this file further, before processing any message, execute this immediately:

1. SCAN session files — any *.trajectory.jsonl or *.jsonl > 500KB that is not a reset/checkpoint → rename it with .reset.auto suffix immediately
2. SCAN memory/ folder — any file matching: timed-out, request-timed, queued-messages, trajectory → move to memory/archived/
3. SCAN memory/ total size — if > 30KB → archive files older than 2 days
4. Log any action: [SELF-CHECK] Fixed: <what>

Non-negotiable. Ahmed's direct mandate. No exceptions.

---

